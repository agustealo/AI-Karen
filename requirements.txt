# AI KAREN runtime dependencies
# Keep this file aligned to live imports/runtime composition. Do not retain retired backends "just in case".
#
# Memory / graph dependency policy:
# - PostgreSQL/Supabase is the canonical durable memory substrate.
# - psycopg is the canonical Python PostgreSQL driver already used by the runtime/data layer.
# - pgvector, pg_trgm, pg_cron, pgTAP, recursive CTE traversal, and RLS are PostgreSQL/Supabase
#   capabilities/extensions and therefore are NOT Python packages to add here.
# - Do not add NetworkX, pgmq clients, Neo4j, Memgraph, FalkorDB, Graphiti, Mem0, Apache AGE
#   clients, or another graph/vector database dependency without a benchmark-backed ADR.
# - Existing KAREN spreading activation must be integrated and benchmarked before considering
#   an external graph-compute library.
# - A graph accelerator, if ever approved, is derived compute/projection only; it never becomes
#   authoritative durable memory.
#
# Removed in DEPENDENCY-TRUTH cleanup: neo4j, kuzu, redis/fakeredis, duckdb, sqlalchemy, asyncpg, hnswlib.
# Active imports that still reference retired dependencies are architecture debt to remove or rewire,
# not justification to re-add the retired packages here.

fastapi==0.136.1
uvicorn
pyautogui
urwid
prompt_toolkit
pydantic
pydantic-settings
python-dotenv
pytest
pytest-timeout
wheel
transformers
sentence-transformers
huggingface_hub
google-generativeai
openai==2.32.0
requests
scikit-learn
spacy
nltk
langdetect
datasets
jinja2
passlib
email-validator
itsdangerous
python-multipart
prometheus_client
cryptography==47.0.0
GPUtil
psutil
matplotlib
PyJWT
PyYAML
markdown
pytesseract
pymupdf
bs4
plotly
seaborn
psycopg
psycopg2-binary
aiofiles
aiohttp
alembic
chardet
httpx==0.28.1
pyotp
tenacity
slowapi
pytest-asyncio
bcrypt
argon2-cffi
copilotkit==0.1.87
sse-starlette
pre-commit
httptools
uvloop
python-json-logger
langgraph==1.1.9
langgraph-sdk==0.3.13
langchain-core
langchain-community
watchdog
jsonschema
opentelemetry-sdk
opentelemetry-api
tiktoken
fastmcp
colorlog
retry
yt_dlp
opencv-python
scenedetect
assemblyai
python-pptx
docx2markdown
chunkr_ai
xmltodict
croniter
mcp
crawl4ai==0.8.6
playwright==1.58.0
