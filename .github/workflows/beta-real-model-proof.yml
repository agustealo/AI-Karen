name: Beta Real Model Proof

on:
  push:
    paths:
      - "src/ai_karen_engine/core/expression/**"
      - "src/ai_karen_engine/core/model_runtime/provider_endpoint.py"
      - "tests/beta/**"
      - ".github/workflows/beta-real-model-proof.yml"
  pull_request:
    paths:
      - "src/ai_karen_engine/core/expression/**"
      - "src/ai_karen_engine/core/model_runtime/provider_endpoint.py"
      - "tests/beta/**"
      - ".github/workflows/beta-real-model-proof.yml"
  workflow_dispatch:

jobs:
  beta-real-model-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install proof dependencies
        run: python -m pip install --upgrade pip pytest pytest-asyncio

      - name: Compile beta model spine
        env:
          PYTHONPATH: src
        run: |
          python -m py_compile \
            src/ai_karen_engine/core/expression/contracts.py \
            src/ai_karen_engine/core/expression/errors.py \
            src/ai_karen_engine/core/expression/engines/builtin_provider_engine.py \
            src/ai_karen_engine/core/model_runtime/provider_endpoint.py

      - name: Run beta real-model wiring proof
        env:
          PYTHONPATH: src
        run: pytest tests/beta/test_real_model_wiring.py -q
